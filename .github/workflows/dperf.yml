name: dperf
on: workflow_dispatch
jobs:
  dperf:
    runs-on: ubuntu-24.04
    steps:
      - name: Check
        run: |
          sudo apt install sysbench -y
          sysbench cpu --cpu-max-prime=20000 --time=60 --threads=$(nproc) run
