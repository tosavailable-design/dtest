name: dperf
on: workflow_dispatch
jobs:
  dperf:
    runs-on: ubuntu-24.04-arm
    steps:
      - run: |
          sudo apt update && sudo apt install btop stress-ng -y
          stress-ng --cpu 0 & btop; killall stress-ng
          git clone https://github.com/baidu/dperf.git
