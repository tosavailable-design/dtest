name: dperf
on: workflow_dispatch
jobs:
  dperf:
    runs-on: ubuntu-24.04-arm
    steps:
      - run: |
          lsmod | grep mlx5
          git clone https://github.com/baidu/dperf.git
          cd dperf
          make
